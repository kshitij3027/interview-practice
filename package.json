{
  "name": "incident-action-board-interview",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "npm run build && node dist/src/server/index.js"
  }
}
